services:
  turso:
    image: ghcr.io/tursodatabase/libsql-server:latest
    container_name: salary-audit-turso
    ports:
      - "8080:8080"
    environment:
      SQLD_NODE: primary
    volumes:
      - turso-data:/var/lib/sqld

volumes:
  turso-data:
